# Progress — 2026-05-08

## Completed

### Homepage Listings — Switch from Static JSON to Live D1 Data
- [x] **Investigated stale homepage data** — "Nos dernières annonces" was reading from 5,259 static WordPress-era JSON files (`src/content/annonces/*.json`), not from the live D1 database synced hourly by cron
- [x] **Attempted SSR approach** — added `prerender = false` + `getActiveAnnonces(db)` to index.astro, but Cloudflare Worker exceeded 3 MiB size limit (content data layer = 35 MB bundled into SSR worker)
- [x] **Reverted SSR** and implemented **pre-build D1→content sync** instead (`scripts/sync-d1-to-content.mjs`):
  - Runs before `astro build` in CI pipeline
  - Queries D1 for all active annonces + photos via `wrangler d1 execute`
  - Writes `*_d1sync.json` files into `src/content/annonces/`
  - Homepage stays prerendered — no worker size issues
- [x] **Fixed CI pipeline** — added `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` to the build step in `.github/workflows/deploy.yml`
- [x] **Fixed zod schema validation** — D1 null values caused `InvalidContentEntryDataError`; updated script to omit null fields instead of writing them

### Duplicate Listings Cleanup
- [x] **Marked overlapping WordPress files as closed** at build time — 252 WP files with `status:'publish'` that have D1 equivalents get patched to `status:'closed'` so only fresh D1 versions appear on homepage
- [x] **Cross-source dedup (reference-based)** — same property in Ubiflow + LBI with different slugs but identical reference (e.g. `MBVAP160009824`). Priority: ubiflow > lbi > wordpress. Removed 29 duplicates.
- [x] **Cross-source dedup (address-based)** — same property with different Ubiflow references at same address+price+surface (e.g. `ATVAP150009722` and `MBVAP160009743` at 4 place du Grand Pascal). Removed 1 additional duplicate.
- [x] **Verified homepage** — 8 unique ventes + 8 unique locations, no duplicates

### LBI Zip Import — Committed to Git
- [x] **Committed orphan local changes** to `kamindu` branch — the LBI `/import-lbi` endpoint code in `workers/cron-sync/index.ts` was never committed to any branch despite being tested previously
- [x] **Confirmed live worker does NOT have `/import-lbi`** — the endpoint was never deployed; previous progress notes were inaccurate

### Hero Search — Type de Bien Filter Fix
- [x] **Root cause identified** — search API did exact match `type_bien = 'Appartement'` but D1 stores raw Ubiflow values like `T3`, `Studio`, `Maison de ville`, `Parking couvert`
- [x] **Fixed search API** (`src/pages/api/search.ts`) — maps dropdown categories to LIKE-based patterns:
  - `Appartement` → `T1-T9, F1-F5, Studio, Duplex%, Triplex%, Loft, Appartement%`
  - `Maison` → `Maison%, Villa%, Propriété%`
  - `Parking` → `Parking%, Garage%, Box%, Stationnement%`
  - etc.
- [x] **Verified** — selecting "Appartement" in location search now returns 24 results (was 0 before)

### Hero Tabs — Estimation Button Relocated
- [x] **Moved "ESTIMATION EN LIGNE"** from first tab (with its own pane) to last position as a link button
- [x] **Links to** `/estimez-le-prix-de-vente-de-votre-bien-en-2mn/#fisher-widget-section` (scrolls to iframe)
- [x] **Default active tab** changed from estimation to "VOIR LES BIENS À LOUER"

### Auto-Redeploy After Cron Sync
- [x] **Added `triggerRedeploy()` to cron-sync worker** — after each successful hourly sync, the worker dispatches a `workflow_dispatch` event to GitHub Actions, triggering a full rebuild+deploy
- [x] **Created GitHub PAT** (fine-grained, `actions:write` on `royboy31/immobiliere-pujol-site`)
- [x] **Set `GITHUB_TOKEN` secret** on cron-sync worker via `wrangler secret put`
- [x] **Added `GITHUB_REPO` var** to `workers/cron-sync/wrangler.jsonc`
- [x] **Deployed** — cron-sync worker now auto-triggers redeploy; homepage listings will refresh hourly
- ⚠️ **Token was exposed in conversation** — needs to be regenerated and re-set

### Data Source Audit
- [x] **Mapped all data sources** across pages:

  | Page | Source | Freshness |
  |---|---|---|
  | Homepage (Nos dernières annonces) | JSON from D1 (pre-build) | Hourly (after auto-redeploy) |
  | /annonces/ventes/ | Live Ubiflow XML (SSR) | Always live (67 listings) |
  | /annonces/locations/ | Live Ubiflow XML (SSR) | Always live (67 listings) |
  | /annonces/[slug] (detail) | D1 database (SSR) | Always live |
  | /api/search | D1 database (SSR) | Always live |

### Memory & Skills
- [x] **Created `/promote` skill** — branch promotion workflow (kamindu → dev → main)
- [x] **Saved branch workflow** preference to memory — never commit directly to main
- [x] **Saved staging URL** to memory — site is at `immobiliere-pujol-staging.roy-68a.workers.dev`

## D1 Database State
- **322 active annonces** total (67 ubiflow, 3 lbi, 252 wordpress)
- **Ubiflow breakdown**: 30 ventes, 37 locations
- **Type de bien**: 50 appartements, 11 parkings, 4 locaux, 2 maisons
- **Cron sync**: running hourly, last sync 08:01 UTC

## Data Flow (Updated)
```
Ubiflow XML → cron-sync worker (hourly) → D1 + R2
                                              ↓
                                   triggerRedeploy() ← NEW
                                              ↓
                              GitHub Actions workflow_dispatch
                                              ↓
                          sync-d1-to-content.mjs (pre-build)
                                              ↓
                          *_d1sync.json files → astro build → homepage
```

## Deploys Today
1. SSR approach — failed (3 MiB worker limit) ✗
2. Revert SSR — success ✓
3. Pre-build D1 sync — failed (missing CF credentials) ✗
4. Add CF credentials to build step — failed (zod null validation) ✗
5. Fix null fields — success, first working deploy ✓ (2m5s)
6. Mark WP overlaps as closed — success ✓ (2m18s)
7. Cross-source dedup by reference — success ✓ (2m12s)
8. Address-based dedup — success ✓ (2m12s)
9. Search filter fix + estimation button — success ✓ (2m19s)
10. Auto-redeploy after cron sync — success ✓ (1m59s)
11. Conditional redeploy (only on changes) — success ✓
12. LBI zip import endpoint to main — success ✓ (2m12s)

### LBI Zip Import — Deployed to Production
- [x] **Cherry-picked LBI commit** (`b3de4e7`) from kamindu to main
- [x] **Verified merge** — fflate import, LBI code, auto-redeploy, conditional rebuild all present in merged file
- [x] **Deployed successfully** — both main worker and cron-sync worker passed
- [x] **Verified live** — `pujol-cron-sync.roy-68a.workers.dev/` now shows `/sync, /status, /import-lbi`
- [x] **Usage flow:**
  1. Upload zip: `wrangler r2 object put pujol-photos/lbi/import.zip --file=<zip>`
  2. Trigger: `curl https://pujol-cron-sync.roy-68a.workers.dev/import-lbi`

## Next Steps
- [ ] **Regenerate GitHub PAT** — token exposed in conversation, needs rotation
- [ ] Test LBI zip import end-to-end with fresh zip on live worker
- [ ] Verify auto-redeploy triggers at next hourly sync (only when listings change)
- [ ] Verify estimation page iframe scrolling from homepage button
- [ ] Mobile responsive testing for updated hero tabs
- [ ] Consider switching /annonces/ventes/ and /annonces/locations/ from Ubiflow XML to D1 (would show LBI + WordPress listings too)
