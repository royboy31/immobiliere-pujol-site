# Progress — 2026-05-06

## Completed

### Branch Alignment & Rebase
- [x] **Fetched origin/main** — Lilanga force-pushed 12 new commits (home page, themed templates, footer rebuild, service pages, header/footer, CTA, nav fixes, favicon)
- [x] **Rebased `develop` onto origin/main** — clean rebase, no conflicts, 4 Kamindu commits on top
- [x] **Rebased `kamindu` onto origin/main** — skipped stale test banner commit (`c8f2b2e`), clean result
- [x] **Force-pushed both branches** to origin
- [x] **Merged develop → main** — fast-forward, deployed successfully (2m0s)

### Hero Search — Interactive AJAX Search from D1
- [x] **Created `/api/search` endpoint** (`src/pages/api/search.ts`)
  - Public GET, no auth required
  - Params: `type` (L/V), `type_bien`, `budget`, `q` (location text), `source` (wordpress/ubiflow)
  - Returns `total` count + 20 results with first photo
  - Budget filter handles loyer_cc for rentals, prix for sales
- [x] **Made hero search bar fully interactive** (`src/pages/index.astro`)
  - **ESTIMATION tab**: VALEUR LOCATIVE is now a dropdown (vente/loyer estimation options), CTA link updates dynamically
  - **LOUER tab**: TYPE DE BIEN dropdown (Appartement, Maison, Local commercial, Bureau, Parking), BUDGET MAX dropdown (500–3000€), location text input
  - **VENDRE tab**: TYPE DE BIEN dropdown (Appartement, Maison, Local commercial, Terrain, Immeuble), BUDGET MAX dropdown (100k–1M€), location text input
  - All inputs trigger AJAX search with 300ms debounce
  - RECHERCHER buttons trigger immediate search
  - Dropdown selection triggers search automatically
- [x] **Results dropdown panel** below search bar
  - Shows listing cards: thumbnail, title, details (type · surface · rooms), location, price
  - Links to `/annonces/{slug}/`
  - Scrollable inner wrapper (`max-height: 400px`) with persistent padding
  - Closes on outside click or tab switch

### Styles & Layout Fixes
- [x] **Dropdown z-index** raised to 9999, results panel to 9998
- [x] **Fixed dropdown clipping** — removed `overflow: hidden` and `isolation: isolate` from `.hero`, set `z-index: 10`
- [x] **Fixed search bar overflow** — added `overflow: visible` to `.hero-search__bar`
- [x] **Estimation CTA link** fixed to `/services/demander-une-estimation-de-prix-de-vente/`
- [x] **Results panel structure** — outer container with fixed padding, inner scrollable div for listing cards

### Photo URL Fix
- [x] **Ubiflow photos were 404ing** — stored as relative R2 keys (`annonces/{slug}/0.jpg`) in D1
- [x] **Fixed in search API** — relative paths now prefixed with R2 public URL (`https://pub-a37eed540afe4dc9b4479da74ba265e1.r2.dev/`)
- [x] **WordPress photos** already had full URLs, kept as-is

### D1 Database Analysis
- [x] **320 active listings** in D1:
  - 252 from WordPress seed (mapped `publish` → `active` by `seed-d1-annonces.mjs` line 26)
  - 68 from Ubiflow sync (36 rentals, 32 sales)
- [x] **Live Ubiflow XML feed** has only 10 listings (6 L, 4 V) — D1 accumulates over time
- [x] **Added `source` filter** to search API for debugging (`?source=ubiflow` or `?source=wordpress`)
- [x] **Added `total` count** to search API response (total matching rows, not just page size)

## D1 Table Structure (Reference)
- `type_annonce`: `'L'` (location/rental) or `'V'` (vente/sale) — NULL for many WordPress entries
- `source`: `'wordpress'` (seeded) or `'ubiflow'` (synced)
- `status`: `'active'` or `'closed'`
- Photos in `annonces_photos` table, linked by `annonce_id`, ordered by `position`

## Deploys Today
All deploys via GitHub Actions (push to main), all successful:
1. Branch rebase + merge (2m0s)
2. Hero search interactive + /api/search endpoint (2m0s)
3. Fix dropdowns, estimation link, input events (2m5s)
4. Z-index fix (2m6s)
5. Add total count to search API (1m51s)
6. Add source filter (2m3s)
7. Photo URL fix (1m52s)
8. Overflow/isolation fix (1m56s)
9. Hero padding adjustment (2m4s)
10. Results panel inner wrapper (2m2s)

## Next Steps
- [ ] Style/polish the results dropdown (design alignment)
- [ ] Mobile responsive testing for hero search
- [ ] Consider filtering search to `source=ubiflow` only (exclude stale WordPress data)
- [ ] Pagination or "see all results" link
- [ ] Homepage redesign continuation
