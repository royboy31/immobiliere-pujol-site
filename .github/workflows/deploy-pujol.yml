name: Deploy to Pujol Account

on:
  workflow_dispatch:

jobs:
  deploy-pujol:
    runs-on: ubuntu-latest
    environment: pujol-staging
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Patch wrangler config for Pujol account
        run: |
          node -e "
            const fs = require('fs');
            const raw = fs.readFileSync('wrangler.jsonc','utf8');
            const cleaned = raw.replace(/^\s*\/\/.*/gm,'').replace(/,(\s*[}\]])/g,'\$1');
            const cfg = JSON.parse(cleaned);
            cfg.d1_databases[0].database_id = '${{ secrets.D1_DATABASE_ID }}';
            fs.writeFileSync('wrangler.jsonc', JSON.stringify(cfg, null, 2));
          "

      - name: Patch R2 public URL for Pujol account
        run: |
          echo "Replacing Roy R2 URL with Pujol R2 URL in src/ and workers/"
          find src/ workers/cron-sync/ -type f \( -name '*.ts' -o -name '*.astro' -o -name '*.md' -o -name '*.json' \) \
            -exec sed -i 's|pub-a37eed540afe4dc9b4479da74ba265e1|pub-ffb425c962e94d71bef4d2fbce95daee|g' {} +

      - name: Sync D1 annonces to content
        run: node scripts/sync-d1-to-content.mjs
        continue-on-error: true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # --- Production indexing switch (go-live) ---
      # Dormant until the GitHub Actions variable ALLOW_INDEXING is set to 'true'
      # on the `pujol-staging` environment. Staging (deploy.yml) is never touched.
      # When 'true', strip the staging-only X-Robots-Tag noindex header so prod
      # can be indexed. robots.txt flips to Allow on its own once the real domain
      # (immobiliere-pujol.fr) is live AND this flag is set.
      - name: Enable indexing for production (go-live switch)
        if: vars.ALLOW_INDEXING == 'true'
        run: |
          echo "ALLOW_INDEXING=true -> removing staging X-Robots-Tag noindex header (public/_headers)"
          rm -f public/_headers

      - run: npm run build
        env:
          NODE_OPTIONS: --max-old-space-size=4096
          ALLOW_INDEXING: ${{ vars.ALLOW_INDEXING }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          OPINIONSYSTEM_API_KEY: ${{ secrets.OPINIONSYSTEM_API_KEY }}
          IMMODVISOR_API_KEY: ${{ secrets.IMMODVISOR_API_KEY }}
          IMMODVISOR_CHECKSUM_IN: ${{ secrets.IMMODVISOR_CHECKSUM_IN }}
          IMMODVISOR_CHECKSUM_OUT: ${{ secrets.IMMODVISOR_CHECKSUM_OUT }}

      - name: Deploy main worker
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Patch email worker config for Pujol account
        run: |
          node -e "
            const fs = require('fs');
            const raw = fs.readFileSync('workers/email/wrangler.jsonc','utf8');
            const cleaned = raw.replace(/^\s*\/\/.*/gm,'').replace(/,(\s*[}\]])/g,'\$1');
            const cfg = JSON.parse(cleaned);
            cfg.account_id = '${{ secrets.CLOUDFLARE_ACCOUNT_ID }}';
            cfg.vars.ALLOWED_ORIGIN = 'https://immobiliere-pujol-staging.pujol.workers.dev';
            fs.writeFileSync('workers/email/wrangler.jsonc', JSON.stringify(cfg, null, 2));
          "

      - name: Deploy email worker
        working-directory: workers/email
        run: rm -rf ../../.wrangler && npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Patch cron-sync worker config for Pujol account
        run: |
          node -e "
            const fs = require('fs');
            const raw = fs.readFileSync('workers/cron-sync/wrangler.jsonc','utf8');
            const cleaned = raw.replace(/^\s*\/\/.*/gm,'').replace(/,(\s*[}\]])/g,'\$1');
            const cfg = JSON.parse(cleaned);
            cfg.d1_databases[0].database_id = '${{ secrets.D1_DATABASE_ID }}';
            cfg.vars = cfg.vars || {};
            cfg.vars.DEPLOY_WORKFLOW = 'deploy-pujol.yml';
            cfg.vars.R2_PUBLIC_URL = 'https://pub-ffb425c962e94d71bef4d2fbce95daee.r2.dev';
            fs.writeFileSync('workers/cron-sync/wrangler.jsonc', JSON.stringify(cfg, null, 2));
          "

      - name: Deploy cron-sync worker
        working-directory: workers/cron-sync
        run: rm -rf ../../.wrangler && npm install && npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Post-deploy sync verification
        run: node scripts/sync-verify.mjs --target pujol
        continue-on-error: true
        env:
          OPINIONSYSTEM_API_KEY: ${{ secrets.OPINIONSYSTEM_API_KEY }}
          MANDRILL_API_KEY: ${{ secrets.MANDRILL_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
