# Progress — 2026-05-01

## Completed

### Sprint 0 Tasks
- [x] **Environment setup** — `.env` file created with all build-time vars (`GTM_ID`, `ALLOW_INDEXING`, `OPINIONSYSTEM_API_KEY`)
- [x] **Fixed `[...slug].astro` double-close bug** — duplicate `</article>` and `</BaseLayout>` tags removed, article footer moved inside `<article>`, estimation style block moved inside `<BaseLayout>`. Fixes invalid HTML on 923 pages. Commit: `cf1d18c`
- [x] **Branch strategy established** — created `develop` branch as integration layer. Flow: `kamindu` → `develop` → `main`
- [x] **Deployment pipeline clarified** — no auto-deploy from GitHub. Cloudflare Worker builds are not connected to Git. Deploy is manual via `npx wrangler deploy`

### D1 + R2 Data Localisation
- [x] **D1 database created** — `pujol-annonces` (ID: `109fb417-c984-4c08-90b2-2a10ae1bc279`) on Roy's Cloudflare account
- [x] **D1 binding added** to `wrangler.jsonc` — binding name `DB`
- [x] **Schema deployed** — 4 tables (`annonces`, `annonces_photos`, `annonces_seo_links`, `sync_log`) + 11 indexes. Executed via `wrangler d1 execute`
- [x] **5,259 annonces seeded to D1** — `scripts/seed-d1-annonces.mjs` reads all JSON files from `src/content/annonces/`, generates SQL, executed against remote D1. 20,964 photos linked. WordPress `publish` status normalized to `active` (252 rows)
- [x] **Sync worker built** — `src/lib/sync-annonces.ts` (Ubiflow → D1 + R2 sync logic), reuses `src/lib/ubiflow.ts` parser
- [x] **D1 query layer built** — `src/lib/db-annonces.ts` replaces `fetchUbiflowAnnonces()` and `getClosedAnnonce()`. Functions: `getActiveAnnonces()`, `getAnnonceBySlug()`, `getRelatedAnnonces()`, `formatDbPrice()`
- [x] **Manual sync API endpoint** — `src/pages/api/sync.ts` (POST, auth via `UPLOAD_TOKEN`)
- [x] **Astro pages updated to use D1** — `index.astro`, `locations.astro`, `ventes.astro`, `[slug].astro` now query D1 instead of live Ubiflow XML. `[slug].astro` has unified model for active + closed annonces with fallback to static assets for annonces not yet in D1
- [x] **Cron worker deployed** — `workers/cron-sync/` is a standalone Cloudflare Worker (`pujol-cron-sync`) with hourly cron trigger (`0 * * * *`). Self-contained: inlined XML parser, shares D1 + R2 bindings. Deployed at `https://pujol-cron-sync.roy-68a.workers.dev`
- [x] **Cloudflare Workers types** — installed `@cloudflare/workers-types`, generated `worker-configuration.d.ts`, updated `tsconfig.json`

## Pending Verification
- [ ] **First cron sync** — cron worker fires at next full hour, check `sync_log` table to confirm:
  ```
  CLOUDFLARE_ACCOUNT_ID=68abcbaf4817943a805737802e15679a npx wrangler@latest d1 execute pujol-annonces --remote --command="SELECT * FROM sync_log ORDER BY id DESC LIMIT 1;"
  ```
- [ ] **Main worker redeploy** — main Astro worker needs rebuild + deploy (`npx wrangler deploy`) to serve pages from D1 instead of live XML

## Not Started
- [ ] Follow up La Boîte Immo (`passerelles@la-boite-immo.com`)
- [ ] Sprint 1 tasks (OpinionSystem reviews)

## Notes
- Cloudflare dashboard confirmed: Worker at `immobiliere-pujol-staging.roy-68a.workers.dev`, runtime vars match `wrangler.jsonc`
- No GitHub Actions or CI pipelines in the repo
- `DEVELOPER_HANDOVER.md` section 10 says "deployed on every push to main" but this is incorrect — builds are not connected
- D1 database is on Roy's account (`68abcbaf4817943a805737802e15679a`) — must set `CLOUDFLARE_ACCOUNT_ID` for wrangler commands since multiple accounts are linked
- Cron worker is separate from main Astro worker because Astro's Cloudflare adapter doesn't support `scheduled` handlers
