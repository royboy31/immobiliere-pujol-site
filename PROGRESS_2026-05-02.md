# Progress — 2026-05-02

## Completed

### Cron Sync Fix
- [x] **Diagnosed cron worker failure** — `sync_log` was empty despite worker being deployed since May 1 20:59 UTC. Root cause: too many individual D1 subrequests per invocation (1,000+ calls for 60 annonces), hitting Cloudflare Worker limits
- [x] **Rewrote sync logic with batched D1 operations** — replaced per-annonce SELECT+UPDATE/INSERT with `INSERT ... ON CONFLICT(slug) DO UPDATE` and `db.batch()` calls in groups of 100
- [x] **Added UNIQUE index on `annonces.slug`** — required for `ON CONFLICT` upsert
- [x] **Added manual trigger endpoints** — `GET /sync` to trigger manually, `GET /status` to view last 10 sync logs
- [x] **Added fatal error logging to D1** — errors now written to `sync_log` instead of silently swallowed
- [x] **Verified sync works** — 60 annonces updated, 47 photos uploaded to R2, 0 errors. Second run confirmed idempotency (0 photos re-uploaded, existing R2 images preserved)
- [x] **Cron worker redeployed** — `pujol-cron-sync` running hourly (`0 * * * *`), first successful sync_log entry recorded

### CI/CD Pipeline
- [x] **GitHub Actions workflow created** — `.github/workflows/deploy.yml` triggers on push to `main` or `develop`
- [x] **Pipeline steps:** `npm ci` → `npm run build` → deploy main worker → deploy cron worker
- [x] **Cloudflare secrets configured** — `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` set as GitHub repo secrets on `royboy31/immobiliere-pujol-site`
- [x] **Fixed cron worker deploy conflict** — wrangler's `.wrangler/deploy/config.json` from main deploy was conflicting with cron worker's `wrangler.jsonc`; resolved by cleaning `.wrangler/` between deploys
- [x] **Pipeline fully green** — both main Astro worker and cron sync worker deploy successfully in ~1m47s

### Main Astro Worker Redeployed
- [x] **Main worker now serves pages from D1** — annonce pages (`index`, `locations`, `ventes`, `[slug]`) query D1 instead of live Ubiflow XML
- [x] **`/api/sync` endpoint live** — manual sync trigger available on the main worker

### Branch Management
- [x] **All branches in sync** — `kamindu` → `develop` → `main`, all fast-forward merged and pushed
- [x] **Branch flow confirmed:** `kamindu` (dev) → `develop` (integration) → `main` (deploy trigger)

## Pending Verification
- [ ] **Hourly cron execution** — check `sync_log` after the next full hour to confirm automated cron is firing:
  ```
  curl -s https://pujol-cron-sync.roy-68a.workers.dev/status
  ```
- [ ] **Staging site smoke test** — verify annonce pages render correctly from D1 at `https://immobiliere-pujol-staging.roy-68a.workers.dev`

## Not Started
- [ ] **Sprint 1 — Task 1.1:** Create `scripts/sync-opinionsystem-reviews.mjs`
- [ ] **Sprint 1 — Task 1.2:** Add reviews section to `src/pages/experts/[slug].astro`
- [ ] **Sprint 1 — Task 1.3:** Update build pipeline to run reviews sync before `astro build`
- [ ] Follow up La Boite Immo (`passerelles@la-boite-immo.com`)

## Notes
- Local builds fail on WSL due to OOM (workerd/tcmalloc needs >1GB, only ~1.3GB free) — CI/CD pipeline solves this
- Cloudflare API token created with permissions: Workers Scripts (Edit), Workers KV (Edit), D1 (Edit), Workers R2 Storage (Edit), Zone (Read)
- Cron sync URLs: `https://pujol-cron-sync.roy-68a.workers.dev/sync` (trigger), `/status` (logs)
