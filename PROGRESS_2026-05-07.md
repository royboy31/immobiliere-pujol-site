# Progress — 2026-05-07

## Completed

### Merge Lilanga's Branch into Develop & Main
- [x] **Reviewed lilanga's branch** — 2 new commits ahead of kamindu:
  - `c6cad42` Themed annonces + filter bar + photo carousel + expert detail rework
  - `0960a5b` Official DPE/GES diagrams + fix diagnostiques parsing
- [x] **Identified 4 conflicts** (all in `src/pages/annonces/`):
  - `[slug].astro` — 5 conflict regions (imports, variables, View type, view construction, template)
  - `index.astro` — 3 conflict regions (data source, header/pills, grid/cards)
  - `locations.astro` — 2 conflict regions (data source, card HTML)
  - `ventes.astro` — 2 conflict regions (data source, card HTML)
- [x] **Root cause**: kamindu branch uses D1 database (`getActiveAnnonces`, snake_case fields), lilanga's uses Ubiflow XML feed (`fetchUbiflowAnnonces`, camelCase fields). Different data sources + different field shapes.
- [x] **Resolved conflicts** with the following strategy:
  - **Listing pages** (index, locations, ventes): Took lilanga's version — ubiflow data + themed `AnnonceCard`/`FilterBar` components. These components are tightly coupled to `UbiflowAnnonce` type.
  - **Detail page** (`[slug].astro`): Combined both — kept kamindu's D1 data layer (`getAnnonceBySlug`, `formatDbPrice`) + lilanga's themed `AnnonceLive`/`AnnonceClosed` components. Mapped D1 related data to `ClosedAnnonceRelated` shape (`titre→title`, `type_annonce→type`, `code_postal→cp`). Added lilanga's extra View fields (`dpeValeurConso`, `dpeValeurGes`, `honorairesChargeAcquereur`, `visiteVirtuelle`). Passed `pool={[]}` since ubiflow list isn't available on D1-powered page.
- [x] **Merged lilanga → develop** — committed as `3a085f6`
- [x] **Merged develop → main** — fast-forward, no conflicts
- [x] **Deployed successfully** — both branches, all steps passed (build, main worker, cron sync worker) in 1m57s

### Lilanga's New Additions (now on main)
- 13 new annonce components: `AnnonceBodyMain`, `AnnonceCard`, `AnnonceClosed`, `AnnonceHero`, `AnnonceLive`, `AnnonceShared`, `ContactForm`, `DpeDiagram`, `DpeScale`, `FilterBar`, `Lightbox`, `SimilarAnnonces`
- New filter logic: `src/lib/annonces-filter.ts` (kind classification, budget filtering, pieces bucketing, arrondissement builder)
- New styles: `_annonces.scss` (2023 lines), `_expert-detail.scss` (562 lines)
- Reworked: `experts/[slug].astro` (expert detail page)
- Updated: `ubiflow.ts` (fixed diagnostiques parsing — nested `bien.diagnostiques` block)
- Asset: `public/images/dpe/dpe-c.png`

### Fix Annonce Detail Page — Photos, DPE, Biens Similaires
- [x] **Fixed photo 404s** (`424fded`) — annonce detail pages were showing broken images (`0.jpg`, `1.jpg` etc.)
  - Root cause: `db-annonces.ts` returned raw photo URLs from D1 without R2 base URL prefix
  - The search API (`/api/search.ts`) already had this fix, but `getAnnonceBySlug` and `getActiveAnnonces` did not
  - Added `resolvePhotoUrl()` helper to `db-annonces.ts` — prepends `https://pub-a37eed540afe4dc9b4479da74ba265e1.r2.dev/` to relative paths, keeps full URLs as-is
- [x] **Added "Biens similaires" section** (`f381c34` → `c03635b`) — was missing because `pool={[]}` was hardcoded
  - First attempt loaded ALL active annonces via `getActiveAnnonces()` — caused **HTTP 500** on every detail page (D1 query too heavy)
  - Fixed by creating `getSimilarPool()` in `db-annonces.ts` — lightweight query that fetches only 6 annonces, preferring same type + code postal, with photos
  - Maps D1 `DbAnnonce` to `UbiflowAnnonce` shape so `SimilarAnnonces`/`AnnonceCard` components work unchanged
- [x] **Confirmed "Performance énergétique" section** renders correctly — was already in code from lilanga's merge, just needed DPE data in D1 (which exists)
- [x] **Verified live** — all three sections (photos, DPE diagrams, biens similaires) confirmed working on staging

### La Boîte Immo (LBI) Zip Import — Worker Endpoint
- [x] **Added `/import-lbi` endpoint** to `pujol-cron-sync` worker (`workers/cron-sync/index.ts`)
  - Reads zip from R2 (`lbi/import.zip`), extracts `Annonces.csv` in-memory using `fflate`
  - Parses LBI CSV format (`!#` delimiter, `latin1` encoding, 335 fields per row)
  - Downloads photos from `staticlbi.com` → uploads to R2 (`annonces/{slug}/{n}.jpg`)
  - Upserts annonces into D1 with `source='lbi'`, same schema as ubiflow sync
  - All processing runs on Cloudflare edge — no local machine needed
- [x] **Installed `fflate`** dependency in cron-sync worker (lightweight zip lib, Workers-compatible)
- [x] **Uploaded test zip** to R2 at `lbi/import.zip` (5.5KB, 3 annonces)
- [x] **Triggered import** — all 3 annonces upserted, 39 photos uploaded to R2, 0 errors
- [x] **Verified in D1** — 3 LBI annonces confirmed active:

  | Reference | Type | Bien | Ville | Prix | Surface | Pièces | Photos |
  |-----------|------|------|-------|------|---------|--------|--------|
  | MBVAP160009824 | V | appartement | Marseille 13006 | 620,000€ | 182m² | 6 | 16 |
  | MBVAP160009826 | V | appartement | Marseille 13004 | 230,000€ | 75m² | 3 | 11 |
  | MBVMA160009827 | V | maison/villa | Marseille 13011 | 360,000€ | 101m² | 4 | 12 |

- [x] **Usage flow:**
  1. Upload zip: `wrangler r2 object put pujol-photos/lbi/import.zip --file=<zip>`
  2. Trigger: `curl https://pujol-cron-sync.roy-68a.workers.dev/import-lbi`

### Merge Lilanga's Commit `969b586`
- [x] **Merged origin/lilanga → main** (fast-forward, no conflicts)
  - Annonce detail page cleanup — dedupe pipe-separated fields, strip ", France", smarter titre generation
  - Split "Caractéristiques" into "Caractéristiques techniques" + "Aspects financiers"
  - Added `meuble` field throughout (ubiflow, db-annonces, filter, detail page)
  - Virtual tour handling — `extractTourUrl()` for description-embedded URLs, `tourEmbedAllowed` flag
  - Contact form text: "Laissez-nous vos coordonnées" / "ENVOYER MA DEMANDE"
  - Footer/Contact page — removed phone/email display, updated hours to `9h–12h / 14h–18h`
  - ExpertCard — strips role from display name, removes phone/profile link
  - New D1 fields in interface: `meuble`, `url_visite_virtuelle`
  - Tour CTA restyled as full-width pill button, contact grid auto-fit
- [x] **Pushed to origin/main** — deployed successfully (2m28s)

## Deploys Today
1. Merge lilanga into develop + main (1m57s) ✓
2. Fix photo 404s — R2 URL prefix in db-annonces.ts (2m6s) ✓
3. Add Biens similaires pool — getActiveAnnonces (1m57s) ✓ → caused 500
4. Fix 500 — replace with lightweight getSimilarPool (2m14s) ✓
5. Deploy cron-sync with LBI import endpoint ✓
6. Merge lilanga `969b586` + deploy (2m28s) ✓

## Next Steps
- [ ] Style/polish the hero search results dropdown
- [ ] Mobile responsive testing for hero search
- [ ] Consider filtering search to `source=ubiflow` only (exclude stale WordPress data)
- [ ] Pagination or "see all results" link
- [ ] Test FilterBar and AnnonceCard on listing pages
- [ ] Set up automated LBI FTP polling (when FTP credentials available from `passerelles@la-boite-immo.com`)
