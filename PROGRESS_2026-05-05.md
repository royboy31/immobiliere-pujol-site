# Progress — 2026-05-05

## Completed

### D1 Database Verified
- [x] **Queried remote D1 database** (`pujol`, ID: `109fb417-c984-4c08-90b2-2a10ae1bc279`) on Roy's Cloudflare account (`68abcbaf4817943a805737802e15679a`)
- [x] **Tables:** `annonces` (5,325 rows), `annonces_photos` (21,470 rows), `annonces_seo_links` (0 rows), `sync_log` (58 entries)
- [x] **Database size:** ~16.8 MB
- [x] **Cron sync confirmed running hourly** — latest sync at 05:02 UTC, processing 62 annonces per run (all updates, no new inserts)
- [x] **No duplicate issue** — D1 uses `ON CONFLICT(slug) DO UPDATE`, R2 uses `bucket.head()` check before upload

### Cron Worker Analysis
- [x] **Reviewed full cron-sync worker** (`workers/cron-sync/index.ts`) — Ubiflow XML → parse → R2 photo upload → D1 upsert → close stale → log
- [x] **Cron trigger was already enabled** in committed code on `kamindu` branch (comment was only in initial git status snapshot)
- [x] **Important: cron sync does NOT trigger a site rebuild** — only updates D1 + R2

### Branch Status Check
- [x] **`main`** = `kamindu` = `develop` all at commit `cf6973c` (before today's changes)
- [x] **`origin/lilanga`** — 4 commits behind main, stopped at `a267055` (handover doc stage)

### Staging Homepage Verified
- [x] **Fetched live staging page** (`immobiliere-pujol-staging.roy-68a.workers.dev`) — full WordPress-style homepage rendering (hero, services, carousels, team, testimonials, blog, newsletter, footer)
- [x] **Template files identified:** `src/pages/index.astro` + `src/content/pages/home.md` (scraped HTML) + `src/styles/theme.scss`

### Test Deploy Attempt
- [x] **Added test banner** to `src/pages/index.astro` to verify correct template
- [x] **Committed to `kamindu`** (commit `c8f2b2e`)
- [x] **Merged to `develop`** and pushed — triggered GitHub Actions deploy (completed successfully)
- [x] **Rolled back immediately** — `git reset --hard main && git push --force` on `develop`
- [x] **Rollback deploy completed** — GitHub Actions rebuilt from `main` (cf6973c)

### Deployment Investigation & Rollback
- [x] **Identified Lilanga's last working deploy** — version `b8c032e2` deployed 2026-05-04 at 15:46 UTC from `lilanga@perelweb.studio`
- [x] **Root cause found: branch mismatch** — Lilanga deploys directly via `wrangler deploy` from her local branch (`origin/lilanga`, which is 4 commits behind `main`). Our GitHub Actions deploy builds from `main` which has D1/R2 changes Lilanga never included.
- [x] **Our deploy broke the CSS** — deploying from `main` via GitHub Actions (commits `cf6973c`) produces different build output vs Lilanga's local build from her branch (`a267055`)
- [x] **User rolled back manually** to Lilanga's version `b8c032e2` via Cloudflare dashboard — site restored

## Issues Found / Root Cause

### Why Deploying from `main` Breaks the Staging Site
- Lilanga has been deploying from her **local branch** (`origin/lilanga`) directly via `wrangler deploy` — NOT through GitHub Actions
- `main` has 4 additional commits beyond Lilanga's branch:
  - `cf1d18c` Fix [...slug].astro duplicate closing tags
  - `6a727db` D1/R2 data localisation + cron sync + CI/CD pipeline
  - `3c1f1a3` Fix cron worker deploy
  - `cf6973c` Fix cron deploy — remove .wrangler/deploy/config.json
- These commits changed annonce page routing and added D1 dependencies, which likely reference CSS classes or produce build output incompatible with the current stylesheet
- **Fix needed:** Align `main` branch code so it builds correctly with full styles, OR merge Lilanga's working state forward

### Wrangler Auth Issues
- Roy's OAuth token expires quickly in non-interactive WSL sessions
- Need to re-run `npx wrangler login` each session

## Key Info

### Deployment Flow
- **GitHub Actions (CI/CD):** Push to `main` or `develop` → `npm run build` → `wrangler deploy` (main + cron workers)
- **Lilanga's flow:** Local build → direct `wrangler deploy` from her branch
- Local builds fail on WSL due to OOM — must use CI/CD
- Worker name: `immobiliere-pujol-staging`

### Rollback Methods
1. Cloudflare dashboard → Workers → immobiliere-pujol-staging → Deployments → Rollback
2. `npx wrangler rollback --version-id <id> --name immobiliere-pujol-staging`
3. Git: reset `develop` to safe commit and force push (triggers rebuild from that state)

### Worker Version History (Key Entries)
- `b8c032e2` — 2026-05-04 15:46 — Lilanga's last working deploy (CURRENT LIVE)
- `14e595f8` — 2026-05-04 11:52 — Lilanga
- `1e8bc7a8` — 2026-05-03 13:26 — Lilanga
- `27085762` — 2026-05-02 11:20 — Kamindu (GitHub Actions, broken CSS)
- `44420076` — 2026-05-01 11:11 — Lilanga

### Cloudflare Accounts (Roy's login)
- `Gilles@hercules-merchandise.com's Account`: `86dfa0e10ca766f79d5042548fc2776f`
- `Kamindudushmantha@gmail.com's Account`: `d6d3df04acc98efe34f43e42636a3dfc`
- `Roy@perelweb.studio's Account`: `68abcbaf4817943a805737802e15679a` (owns pujol D1/R2/Workers)

### D1 Database Name Mismatch
- `wrangler.jsonc` references `database_name: "pujol-annonces"` but actual D1 name is `pujol` — works because wrangler resolves by `database_id`

## Next Steps
- [ ] **Investigate CSS difference** between Lilanga's branch build vs `main` branch build — what's missing/different in the styles
- [ ] **Align branches** — either bring Lilanga's changes into `main` or fix `main` to build with correct styles
- [ ] **Disable GitHub Actions deploy trigger on `develop`** until the branch mismatch is resolved (to prevent accidental broken deploys)
- [ ] Homepage redesign
- [ ] Sprint 1 tasks (reviews sync, expert pages, build pipeline)
- [ ] Follow up La Boite Immo
